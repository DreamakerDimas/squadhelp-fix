import React from 'react';

const EventComponent = ({ name, startDate, endDate, notificationDate }) => {
  console.log(name, startDate, endDate, notificationDate);
  return <div></div>;
};

export default EventComponent;
